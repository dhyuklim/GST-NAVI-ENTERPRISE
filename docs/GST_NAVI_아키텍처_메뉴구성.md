# GST NAVI — 시스템 아키텍처 및 메뉴 구성

**작성일**: 2026-04-03  
**시스템**: GST NAVI v2 (22,447 LOC / 32 컴포넌트)  

---

## I. 시스템 아키텍처

### 1. 전체 구조

```
┌──────────────────────────────────────────────────────────────────┐
│                      사용자 (브라우저)                             │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────────────┐ │
│  │ 대시보드  │  │ 분석도구  │  │ 슬라이드  │  │ 관리 (BI Studio) │ │
│  │ 9개 탭   │  │ 인터랙티브│  │ 쇼 V1/V2 │  │ 리포트/커스텀    │ │
│  └────┬─────┘  └────┬─────┘  └────┬─────┘  └────┬─────────────┘ │
│       └──────────────┴──────────────┴──────────────┘              │
│                              │                                    │
├──────────────────────────────┼────────────────────────────────────┤
│                    React 19 + TypeScript                          │
│  ┌─────────────┐  ┌───────────────┐  ┌────────────────────────┐  │
│  │ ECharts 6   │  │ Motion.js     │  │ react-grid-layout      │  │
│  │ (31+ 차트)  │  │ (애니메이션)   │  │ (드래그&드롭 위젯)      │  │
│  └─────────────┘  └───────────────┘  └────────────────────────┘  │
│                              │                                    │
├──────────────────────────────┼────────────────────────────────────┤
│                     API Layer                                     │
│  ┌──────────────────────────────────────────────────────────┐    │
│  │  Vercel Serverless Functions (api/)                       │    │
│  │  ├── /api/auth/login     JWT 인증                        │    │
│  │  ├── /api/auth/me        토큰 검증                       │    │
│  │  ├── /api/status         데이터 상태                      │    │
│  │  ├── /api/sales          매출 데이터 (필터링)             │    │
│  │  └── /api/summary        요약 데이터                      │    │
│  └──────────────────────────────────────────────────────────┘    │
│                              │                                    │
├──────────────────────────────┼────────────────────────────────────┤
│                     Data Layer                                    │
│  ┌──────────┐  ┌──────────────┐  ┌─────────────────────────┐    │
│  │ JSON 파일 │  │ localStorage │  │ 엑셀 업로드              │    │
│  │ (정적)    │  │ (설정/분석)  │  │ (5시트 파서+판관비배부)  │    │
│  └──────────┘  └──────────────┘  └─────────────────────────┘    │
└──────────────────────────────────────────────────────────────────┘
```

### 2. 프론트엔드 컴포넌트 아키텍처

```
App.tsx (라우터 + 인증 + 상태관리)
│
├── TopNav.tsx ─────────────── 상단 네비게이션 (9탭 + 슬라이드쇼 + 알림)
│   ├── NotificationCenter.tsx ── KPI Alert 벨 아이콘
│   └── [슬라이드쇼] [VER2] 버튼
│
├── FilterBar.tsx ──────────── 접이식 필터 (재무회계 전용)
│
├── GuideTour.tsx ─────────── 첫 방문 온보딩 (6단계)
│
├── SlideShowV2.tsx ────────── 시네마틱 슬라이드쇼 (10슬라이드)
│
├── DrillDownModal.tsx ─────── 공통 드릴다운 모달 (스택 기반)
│   ├── DrillKPI ──────────── KPI 카드 컴포넌트
│   ├── DrillRow ──────────── 클릭 가능 테이블 행
│   └── StatusBadge ───────── 상태 배지
│
├── InteractiveChartModal.tsx ── 인터랙티브 차트 분석기
│   ├── 그룹핑 (7종)
│   ├── 정렬 (4종)
│   ├── TopN / 차트타입 / 추세선
│   ├── 클릭 제외 + 라벨 클릭
│   └── 매출·이익율 슬라이더
│
├── ChartFullscreen.tsx ────── 차트 전체화면 래퍼
│
└── 각 대시보드 컴포넌트 (아래 메뉴 트리 참조)
```

### 3. 데이터 흐름

```
┌─────────────┐     ┌──────────────┐     ┌────────────────┐
│ 엑셀 파일   │────▶│ 엑셀 파서     │────▶│ JSON (sales)   │
│ (.xlsx)     │     │ (5시트 파싱)  │     │ JSON (summary) │
│             │     │ + 판관비 배부  │     └───────┬────────┘
└─────────────┘     └──────────────┘             │
                                                  ▼
                    ┌──────────────────────────────────────┐
                    │        Vercel Serverless API          │
                    │  /api/sales → 필터링 → JSON 응답      │
                    └──────────────┬───────────────────────┘
                                   │
         ┌─────────────────────────┼─────────────────────────┐
         ▼                         ▼                         ▼
┌─────────────┐          ┌─────────────┐          ┌──────────────┐
│ useSalesData│          │ SlideShowV2 │          │ BI Studio    │
│ (훅)        │          │ (실데이터)   │          │ (집계/분석)   │
└──────┬──────┘          └─────────────┘          └──────────────┘
       │
       ▼
┌──────────────────────────────────────────────────┐
│              각 대시보드 컴포넌트                   │
│  ├── KPI 카드 (실/목업)                           │
│  ├── ECharts 차트 (31+)                          │
│  ├── 데이터 테이블                                │
│  └── 드릴다운 모달 (3단계)                         │
└──────────────────────────────────────────────────┘
```

---

## II. 메뉴 구성 전체 트리

### 최상위 메뉴 (TopNav — 9개 탭)

```
GST NAVI
│
├── 1. 메인 ─────────────────────────── MainDashboard.tsx
├── 2. 재무회계 ─────────────────────── Dashboard.tsx
├── 3. 영업/CRM ─────────────────────── CRMDashboard.tsx
├── 4. 생산 ─────────────────────────── ProductionDashboard.tsx
├── 5. 구매 ─────────────────────────── PurchaseDashboard.tsx
├── 6. 서비스 ──────────────────────── ServiceDashboard.tsx
├── 7. 품질 ─────────────────────────── QualityDashboard.tsx
├── 8. HR ───────────────────────────── HRDashboard.tsx
└── 9. 관리 ─────────────────────────── AdminLayout.tsx
    ├── 대시보드 설정 ───────────────── AdminEditor.tsx
    ├── BI Studio ──────────────────── BIStudio.tsx
    ├── 리포트 빌더 ────────────────── ReportBuilder.tsx
    ├── 시스템 ─────────────────────── SystemSettings.tsx
    └── 커스텀 대시보드 ────────────── CustomDashboard.tsx
```

### 부가 기능 (오버레이)
```
├── 슬라이드쇼 V1 ──────────────────── (기존 대시보드 16:9 순환)
├── 슬라이드쇼 V2 ──────────────────── SlideShowV2.tsx (시네마틱 10슬라이드)
├── 인터랙티브 분석 ────────────────── InteractiveChartModal.tsx
├── 가이드 투어 ────────────────────── GuideTour.tsx
└── 알림 센터 ──────────────────────── NotificationCenter.tsx
```

---

## III. 메뉴별 상세 트리

### 1. 메인 (CEO 통합 대시보드)

```
메인
├── CEO 핵심 KPI 테이블 ──────────────── 6종 (매출/이익/OTD/예지보전/FF/재고)
│   ├── [LIVE] 실데이터 라벨
│   └── 각 KPI 클릭 → 드릴다운
│       ├── Level 1: 지표별 상세 분석
│       └── Level 2: 고객/제품 브레이크다운
│
├── 차트 3종 ──────────────────────────── 각 차트 클릭 → 드릴다운
│   ├── 매출·영업이익 월별 트렌드 [LIVE]
│   │   └── 클릭 → 월별 고객/제품 브레이크다운
│   ├── 납기율/생산CAPA/재고/모듈L/T 상관관계
│   │   └── 클릭 → 지표별 월간 추이 + AI 인사이트
│   └── 부문별 건강도 레이더
│       └── 클릭 → 부문별 KPI 진행률 + 핵심 이슈
│
├── 부서카드 7종 ──────────────────────── 클릭 → 해당 탭 이동
│   ├── 재무 (매출 + GP%)
│   ├── 영업/CRM (수주달성률 + Backlog)
│   ├── 생산 (OTD + 리드타임)
│   ├── 구매 (외주조달율 + 자재부족률)
│   ├── 서비스 (예지보전율 + 장비가동율)
│   ├── 품질 (불량률 + Field Fail)
│   └── HR (인력생산성 + 이직률)
│
├── AI 인사이트 ────────────────────────── 실데이터 기반 자동 생성
└── 핵심 실행과제 4건
└── 성장 5대 관점
```

### 2. 재무회계 (실 데이터)

```
재무회계
├── 필터 바 (접이식) ──────────────────── 제품군/채널/고객/프로젝트/공정/국가
│
├── 기간 선택 ─────────────────────────── 최신/전체/월별 + [비교] 모드
│   └── 비교 모드 → 2기간 KPI 증감 표시 (▲▼ %)
│
├── KPI 4종 ───────────────────────────── 각 클릭 → 드릴다운
│   ├── 총 매출액 [LIVE]
│   ├── 매출총이익 [LIVE]
│   ├── 총 수량 [LIVE]
│   └── 평균 영업이익율 [LIVE]
│   └── 드릴다운 (탭 분리):
│       ├── [요약] 고객 Top10 바 + 원가도넛
│       └── [고객 상세] 7열 테이블 → Level 2
│
├── 차트 영역 ─────────────────────────── 각 [분석] 버튼 → 인터랙티브 모달
│   ├── 고객사별 매출단가 및 수익성 ──── [분석] 버튼
│   │   └── 차트 클릭 → 고객 분석 모달 (탭 4개)
│   │       ├── [수익 요약] 제품 도넛 + 워터폴
│   │       ├── [제품별 분석] 11열 테이블 → Level 3
│   │       ├── [원가 구조] 원가/R&D/유지보수/판관비 바
│   │       └── [영업 채널] 채널 카드 + 담당자 테이블
│   │
│   ├── 제품별 매출단가 및 수익성 ──── [분석] 버튼
│   │   └── 차트 클릭 → 제품 상세 (Level 3)
│   │       ├── KPI 6종 (대당 매출/원가/마진)
│   │       ├── 월별 매출·이익 추이 차트
│   │       ├── 프로젝트별 테이블
│   │       └── 거래 로우데이터 (14열)
│   │
│   ├── 판가 트렌드 (ASP) ─────────── [신규, 미푸시]
│   │   └── 대당 매출단가/원가/영업이익 월별 라인
│   │
│   ├── 수익 구조 분석 ────────────── [분석] 버튼
│   │   └── 클릭 → 비용구조 상세 (탭 2개)
│   │       ├── [고객별] 고객사별 비용 테이블
│   │       └── [제품군별] 제품군별 비용 테이블
│   │
│   └── 제품군별 수익성 비교 ─────── [분석] 버튼
│       └── 클릭 → 제품군 상세 (탭 3개)
│           ├── [요약] 9 KPI + 채널 비교
│           ├── [제품별] 제품 테이블
│           └── [고객별] 고객 테이블
│
├── 고객사별 실적 테이블 ──────────────── 행 클릭 → 고객 분석 모달
│
├── [인터랙티브 분석 모달] ────────────── 전체화면 오버레이
│   ├── 그룹핑: 고객/고객그룹/제품군/제품명/채널/국가/영업팀/프로젝트/월별
│   ├── 정렬: 매출순/이익순/이익율순/수량순
│   ├── Top N: 전체/5/10/15/20
│   ├── 차트타입: Bar/Line/Pie
│   ├── 클릭 제외 (바 + X축 라벨)
│   ├── 매출 범위 슬라이더
│   ├── 이익율 범위 슬라이더
│   ├── 추세선 ON/OFF
│   └── 하단 KPI 8종 (실시간 집계)
│
└── 업로드 모달 ───────────────────────── 엑셀 업로드 + 표준양식 다운로드
```

### 3. 영업/CRM (목업)

```
영업/CRM
├── KPI 6종 ───────────────────────────── 각 클릭 → 드릴다운
│   ├── 목표수주액 ₩500억
│   ├── 실제수주액 ₩420억
│   ├── 수주달성률 84%
│   ├── 수주잔량(Backlog) ₩850억
│   ├── 신규고객 수 8
│   └── 고객유지율(CRR) 93%
│
├── 차트 4종 ───────────────────────────── 각 클릭 → 드릴다운
│   ├── 월별 수주 트렌드 (목표 vs 실적)
│   ├── 주요고객 매출비중 (도넛)
│   ├── 고객별 매출 랭킹 (수평 바)
│   └── (이전: 집중도 리스크 — 제거됨)
│
└── 수주 파이프라인 테이블 ────────────── 행 클릭 → 드릴다운
    └── Level 2: 주문 상세 + 마일스톤 → Level 3: 서브아이템
```

### 4. 생산 (목업)

```
생산
├── KPI 6종 ───────────────────────────── 각 클릭 → 드릴다운
│   ├── 전체 오더 100건
│   ├── 납기준수 92건
│   ├── OTD(납기율) 92%
│   ├── 지연 건수 8건
│   ├── 평균 리드타임 24.7일
│   └── 생산성지수 98%
│
├── 차트 4종
│   ├── 월별 OTD 트렌드 (바+라인)
│   ├── 공정별 리드타임 (수평 바)
│   ├── 외주 vs 내부 비율 (도넛) ─── 클릭 → 업체별/생산라인별
│   └── 자재부족 현황 (카드)
│
├── 납기 지연 현황 테이블 ─────────────── 행 클릭 → 드릴다운
│
└── 라인별 캐파 대비 가동률 [신규, 미푸시]
    ├── 5개 라인 (Module Assy ~ 포장/출하)
    └── 캐파 vs 실적 이중바 + 가동률 라인
```

### 5. 구매 (목업)

```
구매
├── KPI 6종 ───────────────────────────── 각 클릭 → 드릴다운
│   ├── 총발주건수 134건
│   ├── 외주조달율 60%
│   ├── 외주품 납기율 89%
│   ├── 자재부족률 5%
│   ├── 자재부족건수 50건
│   └── 긴급발주비율 8%
│
├── 차트 4종
│   ├── 월별 발주/납품 트렌드
│   ├── 외주업체 납기준수율 (수평 바)
│   ├── 자재부족 추이 (스택 바)
│   └── 발주금액 추이 (라인)
│
└── 발주 상세 테이블 ──────────────────── 행 클릭 → 드릴다운
```

### 6. 서비스 (목업)

```
서비스
├── KPI 6종 ───────────────────────────── 각 클릭 → 드릴다운
│   ├── 총 장애콜 58건
│   ├── 고객 Fab 수 6곳
│   ├── 재발 장애콜 6건
│   ├── 긴급 콜(Down) 9건
│   ├── 예지보전율 22%
│   └── 장비가동율 78%
│
├── 차트 4종
│   ├── 장애콜 유형별 (수평 바)
│   ├── 장비가동율 (프로그레스 바)
│   ├── 월별 출장/콜 트렌드
│   └── 예지보전 장비 상태 (도넛)
│
├── 현장출장 내역 테이블 ──────────────── 행 클릭 → 드릴다운
├── 부품재고 현황 테이블
│
├── 법인별 설치 장비 현황 [신규, 미푸시]
│   ├── 5개 법인 (한국/중국/미국/대만/일본)
│   └── 장비 수 + 서비스 인원 + 인당 커버리지
│
├── 워런티 KPI 4종 [신규, 미푸시]
│   ├── 총 설치 장비 1,010대
│   ├── 워런티 커버리지 31.7%
│   ├── 만료 임박 45대
│   └── 평균 인당 커버리지 12.9대/인
│
└── 워런티 차트 [신규, 미푸시]
    ├── IN/OUT 현황 (도넛)
    └── 월별 만료 트렌드 (바+라인)
```

### 7. 품질 (목업)

```
품질
├── KPI 6종 ───────────────────────────── 각 클릭 → 드릴다운
│   ├── 불량률 2.5%
│   ├── Field Fail Rate 1.6%
│   ├── 장애콜 47건
│   ├── 예지보전율 22%
│   ├── 장비가동율 78%
│   └── 개선완료율 86%
│
├── 차트 4종
│   ├── 월별 불량률 트렌드 (라인+목표)
│   ├── 불량 유형 Pareto (바+누적라인)
│   ├── 장애콜 유형별 (수평 바)
│   └── 공정별 가동율 (프로그레스 바)
│
├── 모델별 Field Fail Rate 테이블
├── 개선과제 현황 테이블 ──────────────── 행 클릭 → 드릴다운
│
└── 개선완료율 월별 + 유형별 프로그레스
```

### 8. HR (목업)

```
HR
├── KPI 6종 ───────────────────────────── 각 클릭 → 드릴다운
│   ├── 인력생산성 5.3 대/인
│   ├── 총 인원 600명
│   ├── 이직률 5.3%
│   ├── 제조 투입 39명
│   ├── 서비스 투입 11명
│   └── 영업 투입 7명
│
├── 차트 4종
│   ├── 월별 인력 생산성 (바+라인)
│   ├── 이직률 부서별 (수평 바)
│   ├── 이직 사유 (도넛)
│   └── 공정별 인력 생산성 (수평 바)
│
├── 제조 인력 투입현황 테이블
└── 근속별 이직률 테이블
```

### 9. 관리

```
관리
├── 9-1. 대시보드 설정 ────────────────── AdminEditor.tsx
│   ├── Step 1: 데이터 업로드
│   ├── Step 2: 컬럼 매핑
│   ├── Step 3: 계산식 설정
│   ├── Step 4: 그리드 설정
│   └── Step 5: 레이아웃 에디터 (react-grid-layout)
│
├── 9-2. BI Studio ────────────────────── BIStudio.tsx
│   ├── [분석 목록] 탭
│   │   ├── 5개 프리셋 분석 (고객별/제품군별/월별/그룹별/프로젝트별)
│   │   ├── 사용자 저장 분석 목록
│   │   └── [+ 새 분석] 버튼
│   │
│   ├── [AI 질의] 탭 ──────────────── NLQueryPanel.tsx
│   │   ├── 자연어 입력 ("삼성전자 매출 분석해줘")
│   │   ├── 규칙 기반 파서 (한국어)
│   │   ├── 자동 차트 생성 + 자연어 답변
│   │   ├── 예시 질의 칩
│   │   └── 질의 히스토리
│   │
│   ├── [자동 인사이트] 탭 ────────── AutoInsight.tsx
│   │   ├── 11개 분석 규칙 (적자/저마진/집중도/이상값 등)
│   │   └── 위험/주의/정보/긍정 4단계 분류
│   │
│   └── 분석 에디터 (차트 빌더)
│       ├── 좌측 패널
│       │   ├── 데이터셋 선택
│       │   ├── 측정값 (4카테고리, 16종)
│       │   │   ├── 수익성: 매출/원가/GP/GP%
│       │   │   ├── 영업: OP/OP%/대당매출단가/대당영업이익
│       │   │   ├── 판관비: R&D/유지보수/판매관리비/총판관비/판관비율
│       │   │   └── 기본: 수량/원가율
│       │   └── 차원 (9종, 클릭→그룹핑 전환)
│       │       ├── 고객사/고객그룹/제품군/제품명
│       │       ├── 채널/국가/영업팀
│       │       └── 프로젝트/월별
│       │
│       ├── 우측 캔버스
│       │   ├── 컨트롤: 그룹핑/정렬/TopN/차트타입/기간필터
│       │   ├── 뷰 전환: [차트] [테이블] [피벗] [크로스탭]
│       │   ├── 차트 (ECharts, 클릭→제외/드릴다운)
│       │   ├── X축 라벨 클릭 → 항목 제외
│       │   ├── 제외 태그 + 전체 복원
│       │   └── 하단 KPI 8종 (실시간 집계)
│       │
│       └── 도구: [저장] [공유 링크] [엑셀] [PDF]
│
├── 9-3. 리포트 빌더 ──────────────── ReportBuilder.tsx
│   ├── 템플릿 목록 (3종: 월간/주간/고객별)
│   ├── 편집기 (좌: 섹션 편집, 우: WYSIWYG 미리보기)
│   │   ├── 섹션 타입: KPI/차트/테이블/텍스트
│   │   ├── 순서 변경 (위/아래)
│   │   └── 실 데이터 기반 미리보기
│   ├── 스케줄 (매일/매주/매월 + 시간 + 수신자)
│   └── PDF 생성 (exportDashboardToPdf)
│
├── 9-4. 시스템 ───────────────────── SystemSettings.tsx
│   ├── [사용자 관리] 탭
│   │   ├── 사용자 목록 (이름/아이디/역할/상태)
│   │   ├── 역할 변경 (admin/manager/viewer)
│   │   └── 사용자 추가/삭제
│   │
│   ├── [권한 설정] 탭
│   │   ├── 탭별 권한 매트릭스 (역할 × 9탭)
│   │   └── 기능별 권한 (업로드/PDF/대시보드편집/사용자관리)
│   │
│   └── [Alert 규칙] 탭
│       ├── 5개 예시 규칙 (매출/OTD/불량률/장애콜/이직률)
│       ├── 활성/비활성 토글
│       └── 규칙 추가/삭제
│
└── 9-5. 커스텀 대시보드 ──────────── CustomDashboard.tsx
    ├── 대시보드 선택 드롭다운 + [새 대시보드]
    ├── 위젯 라이브러리 (14종)
    │   ├── KPI 6종 (매출/영업이익/GP%/OP%/수량/건수)
    │   ├── 차트 6종 (고객바/제품바/월별라인/고객파이/채널바/이익율바)
    │   └── 테이블 2종 (고객실적/제품실적)
    ├── 드래그&드롭 배치 (react-grid-layout)
    ├── 미리보기 모드
    └── 저장/불러오기 (localStorage)
```

### 슬라이드쇼

```
슬라이드쇼 V1 (기존 대시보드 순환)
├── 8개 탭 순환 (15초 간격)
├── 전체화면 모드
├── 16:9 최적화
└── ESC로 종료

슬라이드쇼 V2 (시네마틱)
├── 10개 슬라이드 (15초 간격)
│   ├── 1. Executive Command Center (6 KPI + 차트 + AI)
│   ├── 2. 매출·수익 분석 [LIVE 실데이터]
│   ├── 3. 영업·CRM 현황
│   ├── 4. 생산 실적
│   ├── 5. 구매·조달
│   ├── 6. 서비스·예지보전
│   ├── 7. 품질 관리
│   ├── 8. 인사·조직
│   ├── 9. EBITDA·재무건전성 [LIVE]
│   └── 10. 전략 요약·AI 인사이트
├── 다크 프리미엄 테마 (#0B1426)
├── 글래스모피즘 카드
├── 프로그레스 바 + 페이지 인디케이터
├── 키보드 (←→ESC)
└── 전체화면 자동 진입/해제
```

---

## IV. RBAC 권한 체계

```
┌──────────────────────────────────────────────────┐
│               역할별 접근 권한                      │
├──────────┬──────────┬──────────┬─────────────────┤
│   탭     │  admin   │ manager  │    viewer       │
├──────────┼──────────┼──────────┼─────────────────┤
│ 메인     │    ✅    │    ✅    │      ✅         │
│ 재무회계 │    ✅    │    ✅    │      ✅         │
│ 영업/CRM │    ✅    │    ✅    │      ✅         │
│ 생산     │    ✅    │    ✅    │      ❌         │
│ 구매     │    ✅    │    ✅    │      ❌         │
│ 서비스   │    ✅    │    ✅    │      ❌         │
│ 품질     │    ✅    │    ✅    │      ❌         │
│ HR       │    ✅    │    ✅    │      ❌         │
│ 관리     │    ✅    │    ❌    │      ❌         │
├──────────┼──────────┼──────────┼─────────────────┤
│ 업로드   │    ✅    │    ✅    │      ❌         │
│ PDF      │    ✅    │    ✅    │      ❌         │
│ 대시보드 │    ✅    │    ❌    │      ❌         │
│ 사용자   │    ✅    │    ❌    │      ❌         │
└──────────┴──────────┴──────────┴─────────────────┘

계정: admin/gst2026! | manager/mgr2026! | viewer/view2026
```

---

## V. 파일 구조

```
src/
├── App.tsx                          # 라우터 + 인증 + 상태
├── main.tsx                         # 엔트리
├── index.css                        # 글로벌 스타일
│
├── components/
│   ├── MainDashboard.tsx            # 메인 (CEO)
│   ├── Dashboard.tsx                # 재무회계
│   ├── CRMDashboard.tsx             # 영업/CRM
│   ├── ProductionDashboard.tsx      # 생산
│   ├── PurchaseDashboard.tsx        # 구매
│   ├── ServiceDashboard.tsx         # 서비스
│   ├── QualityDashboard.tsx         # 품질
│   ├── HRDashboard.tsx              # HR
│   ├── AdminEditor.tsx              # 관리 - 대시보드 설정
│   ├── TopNav.tsx                   # 상단 네비
│   ├── FilterBar.tsx                # 접이식 필터
│   ├── Sidebar.tsx                  # (미사용, FilterBar로 대체)
│   ├── LoginPage.tsx                # 로그인
│   ├── DetailModal.tsx              # (미사용, DrillDown으로 대체)
│   ├── DrillDownModal.tsx           # 드릴다운 모달
│   ├── ChartFullscreen.tsx          # 차트 전체화면
│   ├── InteractiveChartModal.tsx    # 인터랙티브 분석
│   ├── SlideShowV2.tsx              # 슬라이드쇼 V2
│   ├── UploadModal.tsx              # 업로드
│   ├── PdfExportButton.tsx          # PDF 버튼
│   ├── PermissionGuard.tsx          # 권한 가드
│   ├── GuideTour.tsx                # 가이드 투어
│   ├── NotificationCenter.tsx       # 알림 센터
│   │
│   └── admin/
│       ├── AdminLayout.tsx          # 관리 레이아웃 (5 서브메뉴)
│       ├── BIStudio.tsx             # BI Studio
│       ├── ReportBuilder.tsx        # 리포트 빌더
│       ├── SystemSettings.tsx       # 시스템 설정
│       ├── CustomDashboard.tsx      # 커스텀 대시보드
│       ├── PivotTable.tsx           # 피벗 테이블
│       ├── CrosstabChart.tsx        # 크로스탭 히트맵
│       ├── NLQueryPanel.tsx         # AI 자연어 질의
│       └── AutoInsight.tsx          # 자동 인사이트
│
├── hooks/
│   ├── useAuth.ts                   # 인증 훅
│   ├── useSalesData.ts             # 매출 데이터 훅
│   ├── usePermissions.ts           # 권한 훅
│   ├── useMediaQuery.ts            # 미디어 쿼리
│   └── useDashboardConfig.ts       # 대시보드 설정 저장
│
├── services/
│   ├── api.ts                       # API 클라이언트
│   └── auth.ts                      # 인증 API
│
├── config/
│   └── permissions.ts               # RBAC 권한 설정
│
├── utils/
│   ├── exportPdf.ts                 # PDF 내보내기
│   └── exportExcel.ts               # 엑셀 내보내기
│
├── data/
│   └── mockData.ts                  # 목업 데이터 + 필터 그룹
│
├── types/
│   └── salesTypes.ts                # 매출 데이터 타입
│
├── types.ts                         # 위젯/대시보드 타입
│
└── lib/
    └── utils.ts                     # cn() 유틸리티

api/                                  # Vercel Serverless Functions
├── auth/
│   ├── login.ts                     # POST /api/auth/login
│   └── me.ts                        # GET /api/auth/me
├── sales.ts                         # GET /api/sales
├── status.ts                        # GET /api/status
├── summary.ts                       # GET /api/summary
└── data/
    ├── sales.json                   # 매출 데이터 (427건)
    └── summary.json                 # 요약 데이터
```

---

**문서 이력:**

| 버전 | 일자 | 내용 |
|:----:|------|------|
| 1.0 | 2026-04-03 | 최초 작성 |
